<%= simple_form_for([:admin,@school], class: 'form-control') do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>
  <%= f.input :number_of_students %>
  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>